import type { IWorkflow } from "@spiderz/shared";import { authorizedHeaders } from "./auth.js";import { currentWorkspaceId } from "./workspace.js";const API=import.meta.env.VITE_API_BASE_URL??"http://localhost:3000";
export async function versions(id:string){const r=await fetch(`${API}/api/workspaces/${currentWorkspaceId()}/workflows/${id}/versions`,{headers:authorizedHeaders()});if(!r.ok)throw Error("Could not load versions.");return r.json() as Promise<{id:string;version_number:number;created_at:string;immutable:boolean;restore_message?:string;message?:string;author_email:string}[]>;}
export async function restore(id:string,version:string){const r=await fetch(`${API}/api/workspaces/${currentWorkspaceId()}/workflows/${id}/versions/${version}/restore`,{method:"POST",headers:authorizedHeaders()});if(!r.ok)throw Error("Could not restore version.");return r.json() as Promise<IWorkflow>;}
export async function version(id:string,versionId:string){const r=await fetch(`${API}/api/workspaces/${currentWorkspaceId()}/workflows/${id}/versions/${versionId}`,{headers:authorizedHeaders()});if(!r.ok)throw Error("Could not load version.");return r.json() as Promise<IWorkflow>;}
